# Shared Backlog (DEPRECATED as of 2026-04-30 v0.4)

**This file is deprecated.** The task queue moved to `cronjobs.json` per v0.4. See `SELF_PROPAGATING_LOOP.md` and `GOAL.md`.

Kept for one migration cycle so nothing breaks mid-flight. Checkers will read `cronjobs.json` going forward. This file will be deleted after both sides confirm the new queue is live.

Kit's pre-migration items (doing: v04-cron-template draft, todo: mirror backlog-first in check_kit.py, todo: deploy push-wake on kit side) are folded into `cronjobs.json` task entries.
